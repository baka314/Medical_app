const Router = require('express')
const router = new Router()
const doctorDiaryController = require('../controllers/doctorDiaryController')

router.post('/',)
router.get('/',)
router.delete('/',)
router.get('/:id',)

module.exports = router