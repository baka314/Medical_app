const Router = require('express')
const router = new Router()
const illnessHistoryController = require('../controllers/illnessHistoryController')

router.post('/',)
router.get('/',)
router.delete('/',)
router.get('/:id',)

module.exports = router